{
  "_doc": "Pre-existing spine debt inherited when services/active/ was seeded from app/main.py's hand-maintained _OPTIONAL_ROUTERS on 2026-07-24 (SOA step 2). These entries mount nothing, duplicate a live route, or crash on import TODAY -- the silent `except/getattr None` loop hid them; the fail-loud generator + include_spine surface them. Listed here so the gates are SATISFIABLE on the seed (they do not fail the PR for conditions it did not introduce) while still failing on any NEW dead/duplicate/broken service. Same discipline as tools/reachability_deferred.json: every entry needs a reason; a STALE entry (now healthy) fails a gate so the list can never go decorative: `known` is staleness-checked by generate_spine.py --strict, and `known_runtime` by tests/ci/smoke_ladder.py tier4 (spine::known_runtime_not_stale), which is where the import that decides it actually happens. These are the Step-6 triage backlog (fix/redirect/delete deliberately, human-gated) -- NOT silently changed here.",
  "known": [
    {"service": "entity_report_exporter", "status": "NO_ROUTER", "reason": "Base module exposes no router (getattr None). Real router is the unmounted orphan entity_report_exporter_router. Dead hand-list entry; mounts nothing today."},
    {"service": "org_api_key_manager", "status": "NO_ROUTER", "reason": "Module exposes no `router` attribute; mounts nothing today. Triage: locate/mount the intended router or drop the entry (Step 6)."},
    {"service": "overview_dashboard_api", "status": "NO_ROUTER", "reason": "Base module exposes no router; route is served by the unmounted orphan mcp_risk_tier_overview_dashboard_api. Dead hand-list entry."},
    {"service": "verdict_watchlist_service", "status": "NO_ROUTER", "reason": "Module exposes no `router` attribute; mounts nothing today. Triage in Step 6."},
    {"service": "dashboard_summary_api", "status": "DUPLICATE_ROUTE", "reason": "Its only route /api/dashboard/summary is shadowed by verdict_breakdown_api (registered earlier). Serves nothing live; redundant. Step-6 decision: drop it or make it canonical."},
    {"service": "verdict_breakdown_api", "status": "DUPLICATE_ROUTE", "reason": "Canonical exemplar (module_from_exemplar's first exemplar); genuinely mounted with 8 routes. Flagged only because it co-declares /api/dashboard/summary with dashboard_summary_api. Keep mounted; the redundant side is dashboard_summary_api."}
  ],
  "known_runtime": [
  ]
}
