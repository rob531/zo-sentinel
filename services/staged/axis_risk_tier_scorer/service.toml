[service]
name = "axis_risk_tier_scorer"
import_path = "services.active.axis_risk_tier_scorer.router"
prefix = "/api"
tag = "axis_risk_tier_scorer"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
