[service]
name = "server_risk_tier_transitions_router"
import_path = "services.active.server_risk_tier_transitions_router.router"
prefix = "/api"
tag = "server_risk_tier_transitions_router"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
