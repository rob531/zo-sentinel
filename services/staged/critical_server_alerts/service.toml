[service]
name = "critical_server_alerts"
import_path = "services.active.critical_server_alerts.router"
prefix = "/api"
tag = "critical_server_alerts"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
