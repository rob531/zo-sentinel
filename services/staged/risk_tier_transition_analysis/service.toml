[service]
name = "risk_tier_transition_analysis"
import_path = "services.active.risk_tier_transition_analysis.router"
prefix = "/api"
tag = "risk_tier_transition_analysis"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
