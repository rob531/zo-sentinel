[service]
name = "risk_tier_derivation"
import_path = "services.active.risk_tier_derivation.router"
prefix = "/api"
tag = "risk_tier_derivation"
origin = "service"
auth = "public"
needs_data_layer = true

[dependencies]
requires = [
    "app",
    "services.library.risk_tier_derivation",
]

[environment]
data_plane = "primary"
cache_enabled = false