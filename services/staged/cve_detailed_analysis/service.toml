[service]
name = "cve_detailed_analysis"
import_path = "services.active.cve_detailed_analysis.router"
prefix = "/api"
tag = "cve_detailed_analysis"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
