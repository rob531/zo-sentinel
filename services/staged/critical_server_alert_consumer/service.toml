[service]
name = "critical_server_alert_consumer"
import_path = "services.active.critical_server_alert_consumer.router"
prefix = "/api"
tag = "critical_server_alert_consumer"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
