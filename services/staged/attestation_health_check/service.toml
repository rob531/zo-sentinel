[service]
name = "attestation_health_check"
import_path = "services.active.attestation_health_check.router"
prefix = "/api"
tag = "attestation_health_check"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.