[service]
name = "scoring_global_drift_api"
import_path = "services.active.scoring_global_drift_api.router"
prefix = "/api"
tag = "scoring_global_drift_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
