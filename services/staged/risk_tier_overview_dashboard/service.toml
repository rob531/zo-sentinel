[service]
name = "risk_tier_overview_dashboard"
import_path = "services.active.risk_tier_overview_dashboard.router"
prefix = "/api"
tag = "risk_tier_overview_dashboard"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
