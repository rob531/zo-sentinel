[service]
name = "risk_evidence_drilldown"
import_path = "services.active.risk_evidence_drilldown.router"
prefix = "/api"
tag = "risk_evidence_drilldown"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
