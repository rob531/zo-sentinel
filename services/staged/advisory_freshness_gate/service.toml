[service]
name = "advisory_freshness_gate"
import_path = "services.active.advisory_freshness_gate.router"
prefix = "/api"
tag = "advisory_freshness_gate"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.