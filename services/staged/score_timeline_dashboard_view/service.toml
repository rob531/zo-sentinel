[service]
name = "score_timeline_dashboard_view"
import_path = "services.active.score_timeline_dashboard_view.router"
prefix = "/api"
tag = "score_timeline_dashboard_view"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
