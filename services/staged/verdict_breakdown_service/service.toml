[service]
name = "verdict_breakdown_service"
import_path = "services.active.verdict_breakdown_service.router"
prefix = "/api"
tag = "verdict_breakdown_service"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
