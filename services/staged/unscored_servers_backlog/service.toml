[service]
name = "unscored_servers_backlog"
import_path = "services.active.unscored_servers_backlog.router"
prefix = "/api"
tag = "unscored_servers_backlog"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
