[service]
name = "scoring_consumer_risk_tier_distribution"
import_path = "services.active.scoring_consumer_risk_tier_distribution.router"
prefix = "/api"
tag = "scoring_consumer_risk_tier_distribution"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
