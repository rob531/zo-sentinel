[service]
name = "risk_tier_scoring_consumer_v2"
import_path = "services.active.risk_tier_scoring_consumer_v2.router"
prefix = "/api"
tag = "risk_tier_scoring_consumer_v2"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
