[service]
name = "perspective_snapshot_query"
import_path = "services.active.perspective_snapshot_query.router"
prefix = "/api"
tag = "perspective_snapshot_query"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
