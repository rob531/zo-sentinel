# requirements-ci.txt -- runtime deps for the hermetic CI smoke ladder
# (tests/ci/run_ci_smoke.py). Deliberately minimal: the ladder must run on a
# stock GitHub runner with no ZoComputer host. Keep pins loose-but-bounded so
# a surprise major release can't silently break the gate.
duckdb>=1.5.4,<2.0
fastapi>=0.138.0,<1.0
uvicorn[standard]>=0.49.0,<1.0
requests>=2.34.2,<3.0
# httpx powers Starlette's TestClient, used by the front-end runner to drive
# ui_server's routes in-process (tests/ci/frontend_runner.py).
httpx>=0.28.1,<1.0
# PyJWT: app-foundation oauth_login_service issues/verifies JWT sessions.
PyJWT>=2.13.0,<3.0
psycopg[binary]>=3.1,<4.0
SQLAlchemy>=2.0.51,<2.1
alembic>=1.13,<2.0
passlib>=1.7,<2.0
pydantic>=2.6,<3.0
