[service]
name = "scoring_backlog_summary"
import_path = "services.active.scoring_backlog_summary.router"
prefix = "/api"
tag = "scoring_backlog_summary"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
