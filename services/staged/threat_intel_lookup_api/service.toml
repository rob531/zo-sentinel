[service]
name = "threat_intel_lookup_api"
import_path = "services.active.threat_intel_lookup_api.router"
prefix = "/api"
tag = "threat_intel_lookup_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
