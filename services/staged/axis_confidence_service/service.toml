[service]
name = "axis_confidence_service"
import_path = "services.active.axis_confidence_service.router"
prefix = "/api"
tag = "axis_confidence_service"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
