[service]
name = "mcp_risk_tier_history"
import_path = "services.active.mcp_risk_tier_history.router"
prefix = "/api"
tag = "mcp_risk_tier_history"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
