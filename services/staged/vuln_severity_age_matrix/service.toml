[service]
name = "vuln_severity_age_matrix"
import_path = "services.active.vuln_severity_age_matrix.router"
prefix = "/api"
tag = "vuln_severity_age_matrix"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.