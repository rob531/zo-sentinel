[service]
name = "server_freshness_heatmap_api"
import_path = "services.active.server_freshness_heatmap_api.router"
prefix = "/api"
tag = "server_freshness_heatmap_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
