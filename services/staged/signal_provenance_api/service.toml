[service]
name = "signal_provenance_api"
import_path = "services.active.signal_provenance_api.router"
prefix = "/api"
tag = "signal_provenance_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
