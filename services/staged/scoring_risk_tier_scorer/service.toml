[service]
name = "scoring_risk_tier_scorer"
import_path = "services.active.scoring_risk_tier_scorer.router"
prefix = "/api"
tag = "scoring_risk_tier_scorer"
origin = "service"
auth = "public"
needs_data_layer = true