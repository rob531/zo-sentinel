[service]
name = "adapter_promotion_ledger"
import_path = "services.active.adapter_promotion_ledger.router"
prefix = "/api"
tag = "adapter_promotion_ledger"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
