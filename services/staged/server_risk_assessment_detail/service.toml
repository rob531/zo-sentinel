[service]
name = "server_risk_assessment_detail"
import_path = "services.active.server_risk_assessment_detail.router"
prefix = "/api"
tag = "server_risk_assessment_detail"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
