[service]
name = "perspective_event_rollup"
import_path = "services.active.perspective_event_rollup.router"
prefix = "/api"
tag = "perspective_event_rollup"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.