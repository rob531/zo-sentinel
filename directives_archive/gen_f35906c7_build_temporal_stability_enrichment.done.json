{
  "task": "build_temporal_stability_enrichment",
  "handler": "generate_file",
  "output_file": "temporal_stability_enrichment.py",
  "complexity": "medium",
  "phase": "12",
  "priority": 0.93,
  "description": "Pure enrichment module exposing compute_score(metadata) that returns (float, dict). Reads multiple metadata fields: registry_source, age_days, download_count, dependency_count, publisher_verified, stars. Must NOT write to DB, import other project modules, make network calls, or use runtime file reads. Must complete in under 2 seconds. Returns evidence dict listing fields used and partial scores. Diagnosed BAD \u2014 flat 50.0 across all servers, zero discrimination. Must introduce meaningful variation across MCP servers."
}