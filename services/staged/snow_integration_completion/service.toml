[service]
name = "snow_integration_completion"
import_path = "services.active.snow_integration_completion.router"
prefix = "/api"
tag = "snow_integration_completion"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
