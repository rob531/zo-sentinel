[service]
name = "mcp_submissions_workflow"
import_path = "services.active.mcp_submissions_workflow.router"
prefix = "/api"
tag = "mcp_submissions_workflow"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.