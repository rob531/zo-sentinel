[service]
name = "service_health_trend_api"
import_path = "services.active.service_health_trend_api.router"
prefix = "/api"
tag = "service_health_trend_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.

[gate]
description = "Service health trend API with data layer access to McpLlmAxisScore and mcp_signal_scores"