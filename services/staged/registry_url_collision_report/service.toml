[service]
name = "registry_url_collision_report"
import_path = "services.active.registry_url_collision_report.router"
prefix = "/api"
tag = "registry_url_collision_report"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
