[service]
name = "rescore_wave_duration_ledger"
import_path = "services.active.rescore_wave_duration_ledger.router"
prefix = "/api"
tag = "rescore_wave_duration_ledger"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
