[service]
name = "import_liveness_probe"
import_path = "services.active.import_liveness_probe.router"
prefix = "/api"
tag = "import_liveness_probe"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.