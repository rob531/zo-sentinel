[service]
name = "cadence_health_summary"
import_path = "services.active.cadence_health_summary.router"
prefix = "/api"
tag = "cadence_health_summary"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.