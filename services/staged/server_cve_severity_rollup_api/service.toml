[service]
name = "server_cve_severity_rollup_api"
import_path = "services.active.server_cve_severity_rollup_api.router"
prefix = "/api"
tag = "server_cve_severity_rollup_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
