[service]
name = "server_critical_axis_servers_api"
import_path = "services.active.server_critical_axis_servers_api.router"
prefix = "/api"
tag = "server_critical_axis_servers_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
