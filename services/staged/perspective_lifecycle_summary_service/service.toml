[service]
name = "perspective_lifecycle_summary_service"
import_path = "services.active.perspective_lifecycle_summary_service.router"
prefix = "/api"
tag = "perspective_lifecycle_summary_service"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
