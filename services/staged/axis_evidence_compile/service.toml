[service]
name = "axis_evidence_compile"
import_path = "services.active.axis_evidence_compile.router"
prefix = "/api"
tag = "axis_evidence_compile"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
