[service]
name = "cve_exposure_risk_overlay"
import_path = "services.active.cve_exposure_risk_overlay.router"
prefix = "/api"
tag = "cve_exposure_risk_overlay"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
