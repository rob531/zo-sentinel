[service]
name = "unscored_servers_backlog_api"
import_path = "services.active.unscored_servers_backlog_api.router"
prefix = "/api"
tag = "unscored_servers_backlog_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.