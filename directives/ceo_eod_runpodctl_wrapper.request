{"task_type": "build", "component": "build_runpodctl_ssh_wrapper", "description": "Build a ZoComputer-side SSH wrapper using runpodctl (installed at /home/workspace/bin/runpodctl) that provides a stable alternative to the RunPod web terminal. Should: install runpodctl if missing (GitHub releases API), configure with zocomputerRPMay, provide run_on_pod(pod_id, command) function that executes a command on a running pod via runpodctl ssh and returns stdout/stderr, include retry logic with 30s backoff for connection refused, log all remote executions. Primary use: running eval_tomorrow.py on RunPod pods without web terminal dependency.", "priority": "medium", "confidence": 0.88}