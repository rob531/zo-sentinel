[service]
name = "update_perspective_membership"
import_path = "services.active.update_perspective_membership.router"
prefix = "/api"
tag = "update_perspective_membership"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.