[service]
name = "server_axis_scores_bulk"
import_path = "services.active.server_axis_scores_bulk.router"
prefix = "/api"
tag = "server_axis_scores_bulk"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.