[service]
name = "tier_transition_audit"
import_path = "services.active.tier_transition_audit.router"
prefix = "/api"
tag = "tier_transition_audit"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
