[service]
name = "signal_evidence_export"
import_path = "services.active.signal_evidence_export.router"
prefix = "/api"
tag = "signal_evidence_export"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
