[service]
name = "score_staleness_summarizer"
import_path = "services.active.score_staleness_summarizer.router"
prefix = "/api"
tag = "score_staleness_summarizer"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
