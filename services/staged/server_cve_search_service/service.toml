[service]
name = "server_cve_search_service"
import_path = "services.active.server_cve_search_service.router"
prefix = "/api"
tag = "server_cve_search_service"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
