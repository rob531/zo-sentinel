[service]
name = "axis_critical_servers_api"
import_path = "services.active.axis_critical_servers_api.router"
prefix = "/api"
tag = "axis_critical_servers_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
