[service]
name = "ask_answer_api"
import_path = "ask_answer_api"
prefix = "/api"
tag = "ask"
origin = "live"
auth = "public"
needs_data_layer = true

# Seeded from app/main.py _OPTIONAL_ROUTERS by tools/seed_active_registry.py.
# Live pre-SOA router: code stays at its module path; this file registers it.
