[service]
name = "advisory_freshness_report"
import_path = "services.active.advisory_freshness_report.router"
prefix = "/api"
tag = "advisory_freshness_report"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.