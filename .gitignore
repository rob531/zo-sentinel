__pycache__/
*.pyc
*.pid
*.log
.env
*.env
.goosehints
.build_registry.json
directives/done/
directives/pending/
*.bak.*

# Schema drift staging: duckdb_schema_uptime_probe writes here, never auto-commits
schemas/.pending_drift/

# graphifyy generated index (regenerated by tools/index_graph.py)
graphify-out/

# runtime state (state_loopback.py) -- must NOT be tracked: host does git reset --hard
# origin/main on refresh, which would revert live build state. Untracked files survive.
test-results.json
PROGRESS.md
app_observed_edges.ndjson
schema/app_graph_observed.sql
feature_completeness.html
graph_directives/
capmap.json
lessons/
