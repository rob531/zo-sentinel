[service]
name = "get_servers_linked_to_vuln_advisory"
import_path = "services.active.get_servers_linked_to_vuln_advisory.router"
prefix = "/api"
tag = "get_servers_linked_to_vuln_advisory"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
