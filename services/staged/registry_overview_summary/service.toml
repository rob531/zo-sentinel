[service]
name = "registry_overview_summary"
import_path = "services.active.registry_overview_summary.router"
prefix = "/api"
tag = "registry_overview_summary"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
