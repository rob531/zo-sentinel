[service]
name = "cadence_job_health_detail"
import_path = "services.active.cadence_job_health_detail.router"
prefix = "/api"
tag = "cadence_job_health_detail"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
