[service]
name = "perspective_snapshot_timeline"
import_path = "services.active.perspective_snapshot_timeline.router"
prefix = "/api"
tag = "perspective_snapshot_timeline"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
