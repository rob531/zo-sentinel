[service]
name = "risk_tier_determination"
import_path = "services.active.risk_tier_determination.router"
prefix = "/api"
tag = "risk_tier_determination"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
