[service]
name = "axis_percentile_comparison"
import_path = "services.active.axis_percentile_comparison.router"
prefix = "/api"
tag = "axis_percentile_comparison"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
