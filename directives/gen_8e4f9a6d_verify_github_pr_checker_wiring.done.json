{
  "task": "verify_github_pr_checker_wiring",
  "handler": "generate_file",
  "output_file": "verify_github_pr_checker_wiring.py",
  "complexity": "medium",
  "phase": "12",
  "priority": 0.85,
  "description": "Integration test for github_pr_checker_wiring.py which is failing Gate 8 (1/3 attempts). Verify: (1) PID file locking works, (2) write_service queries return expected format, (3) heartbeat fires at correct interval, (4) signal handling shutdown is clean. Test against synthetic GitHub webhook payload. If it fails, identify whether the failure is in the wiring file or in the underlying github_pr_checker.py module. Do not propose rebuilds of github_pr_checker.py (protected).",
  "reads": [
    "github_pr_checker_wiring.py"
  ]
}