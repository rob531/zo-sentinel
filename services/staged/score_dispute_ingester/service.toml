[service]
name = "score_dispute_ingester"
import_path = "services.active.score_dispute_ingester.router"
prefix = "/api"
tag = "score_dispute_ingester"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
