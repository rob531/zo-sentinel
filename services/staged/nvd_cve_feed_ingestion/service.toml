[service]
name = "nvd_cve_feed_ingestion"
import_path = "services.active.nvd_cve_feed_ingestion.router"
prefix = "/api"
tag = "nvd_cve_feed_ingestion"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.