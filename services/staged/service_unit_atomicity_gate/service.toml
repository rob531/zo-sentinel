[service]
name = "service_unit_atomicity_gate"
import_path = "services.active.service_unit_atomicity_gate.router"
prefix = "/api"
tag = "service_unit_atomicity_gate"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
