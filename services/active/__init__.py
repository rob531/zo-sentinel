# Package marker. Pre-SOA live routers registered here point at their existing
# root module via service.toml import_path and are NOT imported as subpackages;
# builder-emitted services carry their own __init__.py and code in-dir.
