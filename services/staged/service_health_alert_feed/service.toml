[service]
name = "service_health_alert_feed"
import_path = "services.active.service_health_alert_feed.router"
prefix = "/api"
tag = "service_health_alert_feed"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.