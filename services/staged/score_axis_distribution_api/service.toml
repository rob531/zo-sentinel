[service]
name = "score_axis_distribution_api"
import_path = "services.active.score_axis_distribution_api.router"
prefix = "/api"
tag = "score_axis_distribution_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
