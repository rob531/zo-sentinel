[service]
name = "axis_critical_multi_flags"
import_path = "services.active.axis_critical_multi_flags.router"
prefix = "/api"
tag = "axis_critical_multi_flags"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
