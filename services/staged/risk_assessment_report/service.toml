[service]
name = "risk_assessment_report"
import_path = "services.active.risk_assessment_report.router"
prefix = "/api"
tag = "risk_assessment_report"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
