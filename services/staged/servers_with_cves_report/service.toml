[service]
name = "servers_with_cves_report"
import_path = "services.active.servers_with_cves_report.router"
prefix = "/api"
tag = "servers_with_cves_report"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.