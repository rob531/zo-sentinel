[service]
name = "axis_model_version_report"
import_path = "services.active.axis_model_version_report.router"
prefix = "/api"
tag = "axis_model_version_report"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
