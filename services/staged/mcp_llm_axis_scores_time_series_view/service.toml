[service]
name = "mcp_llm_axis_scores_time_series_view"
import_path = "services.active.mcp_llm_axis_scores_time_series_view.router"
prefix = "/api"
tag = "mcp_llm_axis_scores_time_series_view"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
