[service]
name = "axis_score_to_risk_tier"
import_path = "services.active.axis_score_to_risk_tier.router"
prefix = "/api"
tag = "axis_score_to_risk_tier"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
