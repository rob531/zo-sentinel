{
  "task": "verify_schema_bootstrap_completeness",
  "handler": "generate_file",
  "output_file": "verify_schema_complete.py",
  "complexity": "low",
  "phase": "12",
  "priority": 0.78,
  "description": "Verification script to confirm all core tables required by spec section 4 exist in DuckDB: mcp_server_registry, mcp_signal_scores, mcp_signal_enrichments, mcp_threat_associations, mcp_risk_register, mcp_attestations, mcp_definition_history. Query information_schema.columns for each table name and assert it exists. Exit 0 if all present, non-zero with missing table names if any are absent.",
  "reads": [],
  "next_directive": {}
}