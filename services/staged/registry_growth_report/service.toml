[service]
name = "registry_growth_report"
import_path = "services.active.registry_growth_report.router"
prefix = "/api"
tag = "registry_growth_report"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.