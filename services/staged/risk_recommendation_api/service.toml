[service]
name = "risk_recommendation_api"
import_path = "services.active.risk_recommendation_api.router"
prefix = "/api"
tag = "risk_recommendation_api"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
