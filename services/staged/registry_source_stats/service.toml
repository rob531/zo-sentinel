[service]
name = "registry_source_stats"
import_path = "services.active.registry_source_stats.router"
prefix = "/api"
tag = "registry_source_stats"
origin = "service"
auth = "public"
needs_data_layer = true

# Emitted by tools/service_decomposer.py. import_path names services.ACTIVE
# because that is where router.py lives AFTER promotion.
